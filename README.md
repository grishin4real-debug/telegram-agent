# 🧠 Telegram Agent - Автоматический агент для публикации постов

Автоматический агент для публикации постов о трансформации подсознания в Telegram канал. Использует AIDA и SMART frameworks для создания продающего контента.

## 🎯 Возможности

- ✅ **Автоматическая публикация постов** каждые 3 дня в 10:00 МСК
- ✅ **AI-генерация контента** о работе с подсознанием
- ✅ **AIDA framework** (Attention, Interest, Desire, Action) для структурирования постов
- ✅ **SMART методология** для постановки целей в контенте
- ✅ **Разнообразие контента** - более 20 различных тем
- ✅ **Истории успеха** для социального доказательства
- ✅ **Гибкая настройка** расписания и параметров
- ✅ **Логирование** всех действий

## 📋 Что делает агент

Агент автоматически:
1. Генерирует уникальные посты о трансформации подсознания
2. Публикует их в ваш Telegram канал по расписанию
3. Использует проверенные маркетинговые frameworks (AIDA + SMART)
4. Ведет лог всех действий и ошибок

## 🛠 Требования

- Python 3.8 или выше
- Telegram бот (токен от @BotFather)
- Telegram канал (где вы администратор)

## 📦 Установка

### 1. Клонируйте или скачайте проект

Скопируйте все файлы в отдельную папку на вашем компьютере.

### 2. Установите зависимости

```bash
pip install python-telegram-bot pytz
```

Или используйте requirements.txt (если создан):
```bash
pip install -r requirements.txt
```

## 🔑 Настройка Telegram бота

### Шаг 1: Создайте бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Мой Агент Подсознания")
4. Придумайте username (должен заканчиваться на `_bot`, например: `my_subconscious_bot`)
5. **BotFather даст вам токен** - сохраните его! Выглядит примерно так:
   ```
   8143120662:AAFuniDLhtehe4ZWUtjMLVjSwqZmKJQ1www
   ```

### Шаг 2: Получите ID вашего канала

**Вариант 1: Используя @username**
Если у вашего канала есть публичное имя (@channel_name), используйте его напрямую.

**Вариант 2: Получить числовой ID**
1. Добавьте бота в ваш канал как администратора (с правами на публикацию)
2. Отправьте любое сообщение в канал
3. Откройте в браузере (замените `YOUR_BOT_TOKEN` на ваш токен):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
4. Найдите в ответе `"chat":{"id":-100123456789,...}`
5. Число `-100123456789` - это ID вашего канала

### Шаг 3: Добавьте бота в канал

1. Откройте ваш Telegram канал
2. Настройки → Администраторы → Добавить администратора
3. Найдите вашего бота и добавьте его
4. **Важно:** Дайте боту право **публиковать сообщения**

## ⚙️ Конфигурация

### Способ 1: Через файл .env (рекомендуется)

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и заполните:
   ```bash
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   TELEGRAM_CHANNEL_ID=@ваш_канал_или_id
   ```

### Способ 2: Через переменные окружения

Linux/Mac:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен"
export TELEGRAM_CHANNEL_ID="@ваш_канал"
```

Windows (PowerShell):
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
$env:TELEGRAM_CHANNEL_ID="@ваш_канал"
```

### Способ 3: Прямо в config.py

Откройте `config.py` и измените значения по умолчанию.

## 🚀 Запуск

### Проверка конфигурации

Сначала проверьте, что всё настроено правильно:

```bash
python config.py
```

Вы увидите текущие настройки и возможные ошибки.

### Запуск агента

```bash
python telegram_agent.py
```

Агент запустится и начнет работу. Вы увидите сообщения:
- ✅ Подключение к Telegram успешно
- 📅 Время следующей публикации
- 📝 Логи о каждой публикации

### Остановка агента

Нажмите `Ctrl+C` для остановки.

## 📝 Структура проекта

```
telegram-agent/
├── telegram_agent.py          # Главный скрипт агента
├── content_generator.py       # AI-генератор контента
├── config.py                  # Конфигурация и настройки
├── post_structure_template.md # Шаблон структуры постов
├── .env.example              # Пример файла с переменными окружения
├── .env                      # Ваши настройки (создайте сами)
├── README.md                 # Эта инструкция
└── telegram_agent.log        # Лог файл (создается автоматически)
```

## ⚙️ Настройки расписания

По умолчанию агент публикует посты **каждые 3 дня в 10:00 по Московскому времени**.

Чтобы изменить:

В файле `.env`:
```bash
POST_HOUR=14              # Публиковать в 14:00
POST_MINUTE=30            # Публиковать в 14:30
POST_INTERVAL_DAYS=5      # Публиковать каждые 5 дней
```

## 🎨 Настройка контента

### Отключить эмодзи

В `.env`:
```bash
USE_EMOJI=false
```

### Отключить хэштеги

В `.env`:
```bash
USE_HASHTAGS=false
```

### Тестовый режим

Для тестирования без реальной публикации:
```bash
TEST_MODE=true
```

## 📊 Логирование

Логи сохраняются в файл `telegram_agent.log` (по умолчанию).

Уровни логирования:
- `DEBUG` - подробная информация для отладки
- `INFO` - основные события (по умолчанию)
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `CRITICAL` - критические ошибки

Изменить уровень в `.env`:
```bash
LOG_LEVEL=DEBUG
```

## 🔧 Решение проблем

### Ошибка: "TELEGRAM_CHANNEL_ID не установлен"

**Решение:** Укажите ID канала в `.env` или переменных окружения.

### Ошибка: "Не удалось подключиться к Telegram"

**Проверьте:**
1. Правильность токена бота
2. Доступ к интернету
3. Не заблокирован ли Telegram API

### Ошибка при публикации поста

**Проверьте:**
1. Бот добавлен в канал как администратор
2. У бота есть права на публикацию сообщений
3. ID канала указан правильно (с @ или со знаком минус)

### Бот не публикует посты

**Проверьте:**
1. Агент запущен и работает
2. Время публикации наступило (смотрите в логах)
3. Нет ошибок в логах

### Как изменить темы постов

Откройте `content_generator.py` и измените массив `POST_TOPICS`.

### Как добавить свои истории успеха

Откройте `content_generator.py` и добавьте новые истории в `SUCCESS_STORIES`.

## 🔄 Автозапуск (опционально)

### Linux (systemd)

Создайте файл `/etc/systemd/system/telegram-agent.service`:

```ini
[Unit]
Description=Telegram Agent для публикации постов
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/проекту
Environment="TELEGRAM_BOT_TOKEN=ваш_токен"
Environment="TELEGRAM_CHANNEL_ID=@ваш_канал"
ExecStart=/usr/bin/python3 telegram_agent.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable telegram-agent
sudo systemctl start telegram-agent
```

### Windows (Task Scheduler)

1. Откройте "Планировщик заданий"
2. Создайте новое задание
3. Триггер: При запуске системы
4. Действие: Запустить программу → `python telegram_agent.py`

### Используя `screen` или `tmux` (Linux)

```bash
screen -S telegram-agent
python telegram_agent.py
# Нажмите Ctrl+A, затем D для отсоединения
```

Вернуться к сессии:
```bash
screen -r telegram-agent
```

## 📈 Улучшения и доработки

### Хотите изменить структуру постов?

Откройте `post_structure_template.md` - там описана вся структура с примерами.

### Хотите добавить AI-генерацию?

В `content_generator.py` есть функция `generate_post()` - можно интегрировать OpenAI API или другой AI сервис.

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте логи в `telegram_agent.log`
2. Запустите `python config.py` для проверки конфигурации
3. Убедитесь, что все зависимости установлены

## 📄 Лицензия

Этот проект создан для автоматизации публикаций в Telegram каналы.
Используйте на своё усмотрение.

---

**Успешной автоматизации! 🚀**